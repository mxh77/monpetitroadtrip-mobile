{
    "build": {
        "development": {
            "android": {
                "buildType": "apk",
                "distribution": "internal",
                "env": {
                    "GOOGLE_API_KEY": "${GOOGLE_API_KEY}",
                    "API_URL": "${API_URL}"
                }
            }
        }
    }
}